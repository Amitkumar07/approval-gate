# approval-gate's core (ApprovalGate, audit log, PII scanning, BlockingBackend)
# has no required dependencies. Install extras for the pieces you need:

# for LangGraphBackend (used by examples/email_agent_demo.py)
langgraph>=0.2.0
langchain-core>=0.3.0

# optional, for richer PII detection -- the package works fine without this,
# falling back to regex-only detection
# presidio-analyzer>=2.2
